const Doctor = require('./doctors');
const Patient = require('./patients'); //for if we to pull information directly from the patients collection

//export the models
module.exports = { Doctor, Patient };